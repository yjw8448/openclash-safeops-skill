# Common Repairs

Always start with `openclash_diagnose.sh`. For DNS symptoms, use `openclash_dns_audit.sh`. For subscription update problems, use `openclash_subscription_health.sh`. For multi-YAML situations, run `openclash_multisub_audit.sh` and stop before writes.
