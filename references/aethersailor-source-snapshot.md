# Aethersailor Source Snapshot Policy

This project records external ideas as human-reviewed snapshots. It must not fetch upstream shell scripts or remote rules at runtime and execute them. If upstream changes, update this snapshot manually and review safety boundaries before changing scripts.
