# Reporting

Use `python3 scripts/openclash_report_writer.py --output-dir . --stdin-notes`. Reports must be redacted by `openclash_redact.py` and should include both `openclash_fix_report.md` and a timestamped snapshot.
