# LuCI Navigation Map

Use LuCI to verify OpenClash status, subscriptions, config file selection, DNS settings, rules, and logs. If SSH is unavailable, do not pretend SafeOps can fix the router remotely.
