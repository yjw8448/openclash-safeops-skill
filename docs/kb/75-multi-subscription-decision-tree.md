# Multi-Subscription Decision Tree

Multiple YAML files or provider URLs require a stop. Preserve one subscription to one config mapping. Ask which target is intended before candidate generation or write.
