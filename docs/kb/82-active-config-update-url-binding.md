# Active Config / Update URL Binding

If the selected config switches back to another provider, inspect `config_path`, `config_update_url`, and auto-update fields. Do not blindly set update URLs. Ask which provider should own updates.
